// pseduo codey - code
var random_result;
var loss = undefined;
var win;

for (var i = 0; i < 4; i++){
   var crystal = $("<div>")
   $(".crystals").append(crystal);
    console.log(eatmyshorts);

}


// game start has 4 crystals and random result 
//  each crystal should have its own random number(1-12)
//  new random number ot be generated evey win/loss to those 4 crystalz
// clicking on crystals should add pts to total
// until it === random result
//if it is >random result, we lost and add decremnt a loss
// if it is === random result, we won increment a win.